import Placeholder from "@/components/Placeholder";

export default function Admin() {
  return (
    <Placeholder
      fase="Fase 5 (rol admin)"
      titulo="Panel de la tienda"
      descripcion="Seccion protegida (rol admin): aprobar/rechazar reclamos de misiones, cargar catalogo y marcar canjes entregados."
    />
  );
}
